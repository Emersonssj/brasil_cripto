import 'package:flutter/material.dart';

class HistoricDataWidget extends StatelessWidget {
  const HistoricDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
